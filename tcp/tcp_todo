1 vxlan
2 docker
3 openvswitch
4 Q can we have the same ip address in different netns ?
5 mtu probing
7 skb_copy_to_page() is not used by any functions any longer.
8 TCP_DEFER_ACCEPT
Linux 提供的一个特殊 setsockopt ,　在 accept 的 socket 上面，只有当实际收到了数据，才唤醒正在 accept
的进程，可以减少一些无聊的上下文切换。 
9 http://www.shellsec.com/tech/80863.html 
10 GSO GRO
11 TCP Pre-Demux
12 Go 

NOTE 1:

sk->lock.owned will be "1" while the backlog is processed.
Exclusive access to the socket is indicated by one of the
following two conditions being true:

1) sk->lock.slock being held
2) sk->lock.owned being non-zero
